{
  "name": "starterkit-export-options-editor",
  "version": "1.0.0",
  "description": "CE.SDK Starterkit - Export Options Editor",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "check:syntax": "tsc --noEmit"
  },
  "dependencies": {
    "@cesdk/cesdk-js": "latest"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^6.0.3"
  }
}
