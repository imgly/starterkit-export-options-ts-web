{
  "name": "starterkit-export-options",
  "version": "1.0.0",
  "description": "CE.SDK Starterkit - Export Options Editor",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "check:syntax": "tsc --noEmit"
  },
  "dependencies": {
    "@cesdk/cesdk-js": "^1.75.0-rc.5"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^6.0.3"
  }
}
